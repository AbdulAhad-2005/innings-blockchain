{
  "buildCommand": "cd ../.. && corepack enable && pnpm install --frozen-lockfile && pnpm --filter @innings/web build",
  "installCommand": "cd ../.. && corepack enable && pnpm install --frozen-lockfile",
  "outputDirectory": ".next",
  "framework": "nextjs"
}