{
  "name": "innings-platform",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @innings/web dev",
    "build": "pnpm --filter @innings/web build",
    "start": "pnpm --filter @innings/web start",
    "lint": "pnpm --filter @innings/web lint",
    "clean": "rm -rf apps/*/node_modules packages/*/node_modules node_modules"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
